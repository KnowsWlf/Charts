{{- if .Values.traefik.enabled }}
---
# HTTP → HTTPS redirect middleware
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "maxkb.fullname" . }}-redirect
  labels:
    {{- include "maxkb.labels" . | nindent 4 }}
spec:
  redirectScheme:
    permanent: true
    scheme: https
---
# HTTP entry (redirect to HTTPS)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "maxkb.fullname" . }}-web
  labels:
    {{- include "maxkb.labels" . | nindent 4 }}
spec:
  entryPoints:
    - {{ .Values.traefik.httpEntryPoint }}
  routes:
    - kind: Rule
      match: Host(`{{ .Values.traefik.host }}`)
      services:
        - name: {{ include "maxkb.fullname" . }}
          port: {{ .Values.service.port }}
      middlewares:
        - name: {{ include "maxkb.fullname" . }}-redirect
---
# HTTPS entry
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "maxkb.fullname" . }}-websecure
  labels:
    {{- include "maxkb.labels" . | nindent 4 }}
spec:
  entryPoints:
    - {{ .Values.traefik.httpsEntryPoint }}
  routes:
    - kind: Rule
      match: Host(`{{ .Values.traefik.host }}`)
      services:
        - name: {{ include "maxkb.fullname" . }}
          port: {{ .Values.service.port }}
  {{- if .Values.traefik.tlsSecretName }}
  tls:
    secretName: {{ .Values.traefik.tlsSecretName | quote }}
  {{- end }}
{{- end }}
